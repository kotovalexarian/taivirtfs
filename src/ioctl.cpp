#include <iostream>
#include <taivirtfs.h>

int main()
{
    std::cout << "TAIVIRTFS_COMMAND_INSPECT = " << TAIVIRTFS_COMMAND_INSPECT << std::endl;
    std::cout << "TAIVIRTFS_COMMAND_MOUNT   = " << TAIVIRTFS_COMMAND_MOUNT   << std::endl;

    return 0;
}
